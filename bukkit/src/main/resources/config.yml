# Velto - Configuration file
# Config generated by Velto 0.3
#
#
# Configure the spawn point related to /spawn command
spawn: null
#
#
# Auto-messaging system
auto-messages:
  enabled: true
  interval-seconds: 120
  random: true
  messages:
    - automsg1
    - automsg2
    - automsg3

#This section is for Chat and Reload Messages
messages:
  chat: "<&#00ffaa%player_name%&r> &7%message%"
  join: "&a%player_name% joined the server!"
  quit: "&c%player_name% left the server!"
  reload: "&6Chat configuration reloaded successfully."
