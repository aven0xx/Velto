#Velto Roadmap

FOR 0.7.5:

  Commands:
  - [PLANNED] Userdata folder for future data of players in a optimize way (caching need to be considered) Local has to be YAML format
  - [PLANNED] /home, /sethome command using the TeleportManager class for teleportation handling. Possibility of setting multiple home with name by player but the default home is name home and is the default one. Stored in the userdata 
  - [PLANNED] /kit system to distribute kits to players configurable in a kits.yml file. Need to support only Vanilla item for now. There will be a /kit command that will support 2 args : arg1 will be the kit and optional arg2 will be the player defaulting to the sender 
